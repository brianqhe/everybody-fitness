<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <a class="navbar-brand">
    <img src="app/assets/images/logo.png" width="30" height="30" class="d-inline-block align-top" alt="">
    <%# <%= root_path, src: "logo.png", width: "30", height: "30", class: "d-inline-block align-top", alt: "site-logo" %>
    <%= link_to "Everybody Fitness", root_path, class: "navbar-brand" %>
  </a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarNav">
    <ul class="navbar-nav">
      <li class="nav-item active">
        <%= link_to "Home", root_path, class: "nav-link active" %>
      </li>
      <li class="nav-item">
        <%= link_to "Search", profiles_path, class: "nav-link active" %>
      </li>
      <li class="nav-item">
        <%# <% if Profile.find_by(user_id: current_user.id) %>
          <%# <%= link_to "Edit Listing", edit_profile_path(user_id: current_user.id), class: "nav-link active" %>
        <%# <% else %>
          <%= link_to "Create Listing", new_profile_path, class: "nav-link active" %>
        <%# <% end %>
      </li>
      <li class="nav-item">
        <% if user_signed_in? %>
            <%# logout button (only show when logged in) %>
            <%= link_to "Log out", destroy_user_session_path, method: "delete", class: "nav-link active" %>
        <% else %>
            <%# log in button - only show this when logged out %>
            <%= link_to "Log in", new_user_session_path, class: "nav-link active" %>
        <% end %>
      </li>
      <li class="nav-item">
        <%= link_to "About", about_path, class: "nav-link active" %>
      </li>
      <li class="nav-item">
        <% if user_signed_in? %>
          <%= link_to "Inbox", conversations_path, class: "nav-link active" %>
        <% end %>
      </li>
    </ul>
    <span class="navbar-text">
      <% if user_signed_in? %>
        <%= current_user.email %>
      <% end %>
    </span>
  </div>
</nav>